# Autoreview

Codex-only, model-free local review discipline for FAST changes or explicitly requested self-review.
It validates concrete findings and hands clean work to deterministic completion checks. It deliberately
does not stack itself on top of an already scheduled Sol, Claude, or security reviewer.

Install by copying `codex/skills/autoreview` to `~/.codex/skills/autoreview`.
